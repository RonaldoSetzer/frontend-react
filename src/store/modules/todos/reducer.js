const initialState = { todos: [1, 2, 3, 4], totalCompleted: 0, total: 0 };

function todo(state = initialState, action) {
  switch (action.type) {
    default:
      return state;
  }
}

export default todo;
